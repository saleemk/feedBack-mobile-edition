# Optional host folder containing fee[dB]ack song libraries.
# Use an absolute path when the folder is outside this repository.
LIBRARY_PATH=./library

# Optional host port. The application remains on port 8000 inside the container.
FEEDBACK_PORT=8000
